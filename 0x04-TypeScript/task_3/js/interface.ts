// Define the RowID type as number
export type RowID = number;

// Define the RowElement interface with required fields
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
